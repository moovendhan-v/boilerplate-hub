type User {
  id: ID!
  email: String!
  name: String
  avatar: String
  boilerplates: [Boilerplate!]!
  createdAt: String!
  updatedAt: String!
}

extend type Query {
  users: [User!]!
  user(id: ID!): User
}